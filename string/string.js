const uppercase = (str) => {
    return str.toUpperCase();
}

//lowercase function to lowercase text
const lowercase = (str) => {
    return str.toLowerCase();
}

module.exports = {
    uppercase,
    lowercase
};